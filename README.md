# 學習打卡 App

一個可以安裝在手機主螢幕的「每日學習打卡」工具，依照你的每週回家學習計畫設計。

## 功能

- **今日**：顯示今天的學習計畫，完成一項就按「打卡」，會蓋上紅色印章、記錄時間；也可以臨時新增自訂項目
- **計畫**：查看完整的每週計畫（平日每天 2 小時、1–2 科；假日最多 5 小時）
- **統計**：連續完整達成天數、本週完成率、累計打卡次數、本週每日進度長條圖
- **記錄**：歷史打卡紀錄（最近 90 天）
- 資料存在手機／電腦瀏覽器的本機（localStorage），不會上傳到任何伺服器

## 檔案說明

| 檔案 | 用途 |
|---|---|
| `index.html` | 主要程式（單一檔案，含全部 HTML/CSS/JS） |
| `manifest.webmanifest` | PWA 安裝資訊（名稱、圖示、全螢幕） |
| `sw.js` | Service Worker，讓 App 可以離線使用 |
| `icon-192.png` / `icon-512.png` | 手機桌面圖示 |
| `_test.js` | 功能測試腳本（可選） |

## 如何開始使用

### 方法一：電腦上直接看
用瀏覽器開啟 `index.html` 即可操作。

### 方法二：手機上使用（推薦）
1. 把整個資料夾放上 **GitHub Pages** 或任何靜態網站託管（Netlify、Vercel 等）
   - GitHub 步驟：把資料夾內容推到一個 repo → 在 repo 的 Settings → Pages → 選 Branch 部署 → 得到網址
2. 用手機瀏覽器打開該網址
3. 點瀏覽器選單 →「加入主螢幕」（Android 為「加到主畫面」／iPhone 為「加入主畫面」）
4. 手機桌面就會出現「學習打卡」圖示，點開像一般 App 一樣全螢幕使用，且離線可用

> 註：PWA 的「安裝到主螢幕」功能需要透過 https 網址開啟；如果只是本機打開 `index.html`，功能全部可用，只是不會出現安裝提示。

## 修改計畫內容

所有學習計畫都定義在 `index.html` 的 `PLAN` 陣列（每個星期天數對應一天的科目、分鐘數、備註），直接改文字即可。

## 執行測試（可選，需安裝 Node.js）

```bash
node _test.js
```

預期輸出 25 項全部 PASS。
